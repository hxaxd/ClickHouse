-- GRANT ON * under a namespace covers the namespace recursively and nothing else
0
0
1
1
-- the stored grant shows the namespace scope
1
-- wildcard row policy operations are rejected under a namespace
1
1
-- CREATE ON CLUSTER authorizes exactly the created table
1
